import React from 'react';
import { View, Text } from 'react-native';

const ParkScreen: React.FC = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Park Screen</Text>
    </View>
  );
};
export default ParkScreen;
